import { StyleSheet } from "react-native";

export default styles = StyleSheet({
	sample: {
		// all the styles will be provided here
	},
	sampleText: {
		// Like this separate all the styles and the main component
	},
});
